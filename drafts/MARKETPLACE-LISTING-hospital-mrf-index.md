# HITL DRAFT ONLY — GitHub Marketplace listing copy (hospital-mrf-index)

Status: HITL DRAFT ONLY. Do **not** open the Marketplace listing UI overnight. Do **not** publish a new listing. This is paste copy for an **EDIT** of the existing hospital-mrf-index card. Rogue does not open listing editor.

Action lives in repo **bennyj121/hospital-price-series** (Marketplace slug hospital-mrf-index). Free utilities / Continuous integration. **No paid SKU. No Ko-fi. No hospital MRF-change extract CTA.**

AI disclosure: an AI (Rogue) drafted this. Benjamin/Atlas reviews before any listing UI.

## Repo
https://github.com/bennyj121/hospital-price-series
Live card: https://github.com/marketplace/actions/hospital-mrf-index (HTTP 200, Latest **v0.1.6**)
uses: `bennyj121/hospital-price-series@v0.1.6`

**v0.1.6** still peels pre-strip files (action.yml description still had leftover paid CTA at tag time). Do **not** retag v0.1.6 overnight. Do **not** create a new release overnight. Morning HITL if a cleaned tag is needed so the card picks up stripped `action.yml`.

## Short description (paste; free utilities; do not lead with a paid signal)

```
Fetch a hospital cms-hpt.txt and write published MRF URLs, optional CPT extract.
```

## About (paste)

Free GitHub Action `hospital-mrf-index`: fetch a hospital `cms-hpt.txt` (45 CFR 180.50) and write published `mrf-url` lines. Optional shoppable extract from a CMS wide CSV already in the workspace (no zip download). Outputs `mrf-count` and `extract-rows`.

uses: bennyj121/hospital-price-series@v0.1.6

Positioning: free utilities / Continuous integration. No paid SKU, no Ko-fi, no hospital MRF-change extract, no extract-request order path.

Built by Rogue, an AI agent, not a human. Not endorsed by CMS or any hospital. Do not email hospital staff listed in an index.

## Steps (UI) — Atlas only, morning HITL
1. Do **not** open listing editor overnight.
2. If the live card must pick up the stripped `action.yml` / README, cut a **new** tag after the strip commit (do **not** move v0.1.6). Otherwise paste short description + About onto the existing hospital-mrf-index card only.
3. Primary category: leave existing (or Continuous integration).
4. Paste the short description above. Confirm branding from action.yml (file-text / blue).
5. Do **not** invent a new Marketplace URL.

## Out
- Rogue opening the listing editor
- Overnight tag / retag v0.1.6 / new release
- Paid CTAs / Ko-fi / hospital MRF-change extract / extract-request / offer.html as a storefront
- Touching cms-hpt-validate or cms-hpt-validator
- Cold email / Reddit / HN / any post
